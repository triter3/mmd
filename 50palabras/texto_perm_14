The and implemented lists; Doubly kept storage it element unrelated 
a doubly-linked lists different following ordering each linked in the 
can as elements  preceding by to they are contain 
locations. element a is link to List the containers the 
internally of the link and of it. each association to 
store element 
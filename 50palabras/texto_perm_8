it by the the storage of and link of association 
List can doubly-linked locations. in element to  containers they 
different preceding each are the Doubly a element it. internally 
is as kept following link linked element lists; store contain 
and elements to unrelated ordering the to The implemented a 
each lists 
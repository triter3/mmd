locations. different List the linked the doubly-linked a it can 
link a unrelated implemented and to the preceding in element 
store lists Doubly the  ordering element it. lists; by 
internally kept is The as storage and elements they each 
of containers to to each link element following of association 
contain are 
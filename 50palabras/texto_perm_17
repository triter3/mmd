element association a as store kept the ordering element List 
link internally each a implemented linked storage the each element 
locations. the The and can is doubly-linked to  lists 
containers of and by unrelated are to contain of they 
to following in lists; elements the Doubly it. different link 
preceding it 
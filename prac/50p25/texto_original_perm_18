unrelated doubly-linked and containers the by different it. each it 
can the to link they each  kept The and 
element contain association Doubly implemented preceding the is element to 
lists; link the elements following locations. in a lists List 
element as of a ordering to of storage internally linked 
store are 
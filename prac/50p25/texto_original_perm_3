 containers to the unrelated by ordering locations. it lists 
the link List of each lists; following contain the storage 
store association internally and implemented elements the a to a 
to are Doubly each in kept doubly-linked different linked The 
element preceding is they it. link can and element element 
of as 
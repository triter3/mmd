to Doubly The linked and a the are contain can 
it element  by is preceding unrelated the element ordering 
each of lists internally and implemented as link they the 
locations. element it. storage different following in to store of 
containers link lists; the List doubly-linked elements each to association 
kept a 
The the to they is store association element unrelated a 
the it. elements lists; different ordering the containers preceding to 
each  kept contain List as internally are each a 
doubly-linked implemented following can element by Doubly and to of 
lists of locations. in link element and storage linked it 
link the 
List the are implemented as doubly-linked lists; Doubly linked unrelated 
can store each of a elements they contain preceding different 
and lists storage a The ordering is kept internally by 
containers association to each element of the link link the 
element in it and locations. to to the element following 
it.  
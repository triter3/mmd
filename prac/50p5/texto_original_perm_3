List the are implemented as doubly-linked lists; element linked unrelated 
can store each of a elements they contain preceding different 
and lists storage a The to is kept it. by 
containers association ordering each element of the link link the 
element in it and locations. to to the Doubly following 
internally  
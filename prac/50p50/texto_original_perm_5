it. a element as a locations. are to storage the 
in of they of the internally the implemented kept unrelated 
link linked by lists Doubly each lists; following store is 
each different element and association contain ordering and element The 
the can  List elements link doubly-linked to containers to 
preceding it 
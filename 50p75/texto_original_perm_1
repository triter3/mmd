each to the List as ordering lists; in linked kept 
can element contain of implemented elements link by and and 
different unrelated it each The lists is store internally to 
the  link Doubly of to a storage they it. 
association preceding are locations. following containers element the element the 
a doubly-linked 
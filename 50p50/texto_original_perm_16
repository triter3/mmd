internally the contain preceding it. and lists; containers element and 
lists a element implemented as kept each Doubly the association 
following store unrelated linked it the ordering to by element 
each The in to List different of locations. storage doubly-linked 
can elements  to they the are link a is 
of link 